# 依赖（176M，绝对不能提交）
node_modules/

# npm 缓存（本机环境特殊配置，238M，绝对不能提交）
.npm-cache/

# 构建产物（Cloudflare Pages 会自己构建）
dist/
.astro/

# 日志与环境变量
*.log
npm-debug.log*
.env
.env.*

# 编辑器与系统
.DS_Store
.idea/
.vscode/
Thumbs.db

# 本机工作区残留（非站点文件，合并回原 .gitignore 避免误提交 133 个模板文件）
blog-template/
.wrangler/
.workbuddy/
# 只忽略仓库根目录下的散落图片，不要写成裸 *.png ——
# 那会把 public/ 下的站点图片资源一起吞掉，而且是静默的：
# 本地检查一切正常，构建产物里却没有这个文件，线上分享图直接裂图。
# 文章封面图同样走 public/，必须能被提交。
/*.png
extract_resume.py
render_resume.py
gh_api_push.py
